{
  "name": "the-last-suspect",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "benchmark": "node src/algorithms/benchmark.js"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.6.1"
  }
}
